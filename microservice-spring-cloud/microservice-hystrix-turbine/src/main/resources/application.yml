
server:
  port: 8031

spring:
  application:
    name:  microservice-hystrix-turbine
###eureka提供者配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password123@localhost:8761/eureka #http://peer1:8761/eureka  
    healthcheck:
      enabled: true  
  instance:
    prefer-ip-address: true
    instanceId: ${spring.application.name}:${spring.application.instance_id:${server.port}}
 ###turbine 的基本配置 
#turbine:
#  aggregator:
#    clusterConfig: MICROSERVICE-CONSUMER-MOVIE-RIBBON-WITH-HYSTRIX,MICROSERVICE-PROVIDER-USER
#  appConfig: microservice-consumer-movie-ribbon-with-hystrix,microservice-provider-user

turbine:
  aggregator:
    clusterConfig: default
  appConfig: microservice-consumer-movie-ribbon-with-hystrix,microservice-consumer-movie-feign-with-hystrix
  clusterNameExpression: "'default'"
